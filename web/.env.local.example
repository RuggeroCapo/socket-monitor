# Local frontend against the deployed dashboard instead of a local Postgres.
REMOTE_DASHBOARD_URL=https://ita-vine-stats.duckdns.org

# Leave unset when using REMOTE_DASHBOARD_URL.
# Only needed if you want the local Next app to query Postgres directly.
# DATABASE_URL=postgres://postgres:changeme@localhost:5432/vinestats
